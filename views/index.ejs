<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href = "https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.0/css/all.min.css">
    <link rel="stylesheet" href = "/css/styles.css">
    <title>YouTube to MP3 Converter :D</title>
</head>
<body>
    <div class="top-container">
        <form action="/convert" method="POST" id="form">
            <h1>YouTube to MP3 Converter</h1>
            <h4>insert requested video link ID here</h4>
            <div>
                <input type="text" name="vidlink" placeholder="enter video ID from YT link"><button id="convert-btn">wololo</button>
            </div>
        </form>
    </div>
    <div class="bottom-container">
        <% if(typeof success != "undefined" && success){ %>
            <div class="success">
                <p><%= video_title%></p>
                <a href="<%= vidlink %>"><button id="download-btn">DOWNLOAD</button></a>
            </div>
        <%}  else if(typeof success != "undefined" && !success) { %>
            <div class = "errors">
                <p><%= message %></p>
            </div>
            <% } %>
    </div>
</body>
</html>